version: '3'
services:
  next:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # アプリが使うポート3002をホストとコンテナで繋ぐ
      - '3002:3002'
    volumes:
      - .:/app
   
    # environment:
    #   - PORT=3003
    command: npm run dev
    # command: tail -f /dev/null
